---
// This is a redirect file to maintain backward compatibility

//I need to export it to prevent type errors. See bug report: https://github.com/withastro/astro/issues/14100
export const defaultLang = "en";

// Redirect to the default language page
return Astro.redirect(`/cv/${defaultLang}/`);
---

<!-- This page is just a redirect to the default language version -->
